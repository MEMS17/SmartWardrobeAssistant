import { StyleSheet } from 'react-native';

export const homeStyles = StyleSheet.create({
    title: {
        fontSize: 24,
        fontWeight: 'bold',
        marginBottom: 10,
    },
    subtitle: {
        marginBottom: 30,
    },
    buttonContainer: {
        width: '80%',
        marginBottom: 15,
    },
});